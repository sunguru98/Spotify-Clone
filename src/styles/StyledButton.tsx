import styled from "styled-components";

export default styled.button`
  background: var(--primary-spotify-color);
  border-radius: 10rem;
  font-size: 1.4rem;
  padding: 1.6rem 1.4rem;
  text-align: center;
  text-transform: uppercase;
  color: white;
  border: none;
  font-weight: 700;
  max-width: 18.2rem;

  & a {
    display: block;
  }
`;
